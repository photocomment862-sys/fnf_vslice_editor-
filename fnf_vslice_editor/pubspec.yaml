name: fnf_vslice_editor
description: FNF V-Slice Editor
publish_to: "none" # Bu, paketini pub.dev'e yüklemez
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

  dependencies:
    flutter:
        sdk: flutter

          # Gerekli paketler
            path_provider: ^2.0.17
              flutter_lints: ^6.0.0
                characters: ^1.4.1
                  material_color_utilities: ^0.13.0
                    meta: ^1.17.0

                    dev_dependencies:
                      flutter_test:
                          sdk: flutter
                            test: ^1.21.0

                            # Flutter assets ve ayarlar
                            flutter:
                              uses-material-design: true

                                assets:
                                    - assets/ # Eğer asset klasörün varsa buraya ekle